<template>
  <v-list-item @click.prevent="toggle(index)">
    <v-list-item-action>
      <v-btn icon @click="openModal()">
        <v-icon color="pink">edit</v-icon>
      </v-btn>
    </v-list-item-action>

    <v-list-item-content>
      <v-list-item-title>{{ task.title }}</v-list-item-title>
      <v-list-item-subtitle>{{ task.sub_title }}</v-list-item-subtitle>
    </v-list-item-content>

    <v-list-item-action-text></v-list-item-action-text>
    <v-icon v-if="task.isCompleted" color="green">check_circle</v-icon>
    <v-icon v-else color="gray">check_circle</v-icon>
  </v-list-item>
</template>

<script>
export default {
  name: "task",
  data() {
    return {};
  },
  props: ["task", "index"],
  mounted() {},
  methods: {
    toggle(index) {
      console.log(index);

      // TODO: push the changes to the store
    },
    openModal() {
      console.log(this.task.id);
      this.$router.push({
        name: "notes",
        params: {
          taskId: this.task.id,
        },
      });
    },
  },
};
</script>

<style>
</style>